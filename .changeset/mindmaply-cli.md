---
"mindmaply-core": minor
---

Add a `mindmaply` CLI (render, validate, share, convert) so shells and AI agents can use the library without writing a Node script. Source comes from a file or stdin; format is auto-detected.
